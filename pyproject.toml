[project]
name = "synapse-ai"
version = "0.1.0"
description = "An extensible AI platform built with LangChain."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.59.2",
]
